import { BusinessUnitLists } from './business-unit-lists';

describe('BusinessUnitLists', () => {
  it('should create an instance', () => {
    expect(new BusinessUnitLists()).toBeTruthy();
  });
});
